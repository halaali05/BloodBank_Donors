/**
 * Notification sounds live in the repo (your copies):
 * - Android: android/app/src/main/res/raw/emergency_request.mp3, normal_request.mp3
 * - iOS: ios/Runner/emergency_request.mp3, normal_request.mp3 (Xcode → Copy Bundle Resources)
 *
 * Source files used: Desktop urgent.mp3 → emergency_request, Desktop normal.mp3 → normal_request.
 */
console.log("Sounds are bundled as emergency_request.mp3 / normal_request.mp3.");
